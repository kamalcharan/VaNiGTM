-- ════════════════════════════════════════════════════════════════════════════
-- 148_ki_txn_import_customer_lookup.sql
-- No-op placeholder. The original file contained only the word "doc" — an
-- abandoned stub that broke the migration runner with a SQL syntax error.
-- Recorded as applied so subsequent migrations can run.
-- If/when this customer-lookup behavior is actually needed during txn import,
-- create a new migration (149_*+) — do not retroactively edit this one.
-- ════════════════════════════════════════════════════════════════════════════
SELECT 1;
