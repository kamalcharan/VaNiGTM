'use client';
import { ComingSoon } from '../../_coming/ComingSoon';

export default function NovaEstatePage() {
  return (
    <ComingSoon
      eyebrow="Nova · Digital"
      title="Fix the digital estate"
      description="N1. Audit what your digital presence actually says, find the gaps, and work through them in order."
      steps={['Audit the estate', 'Score the gaps', 'Fix in priority order', 'Re-audit']}
    />
  );
}
