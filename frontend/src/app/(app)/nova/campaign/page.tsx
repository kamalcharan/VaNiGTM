'use client';
import { ComingSoon } from '../../_coming/ComingSoon';

export default function NovaCampaignPage() {
  return (
    <ComingSoon
      eyebrow="Nova · Digital"
      title="Run a campaign"
      description="N2. Take a fixed estate to market — plan, produce, publish and measure as one pathway rather than four tools."
      steps={['Plan the campaign', 'Produce the assets', 'Publish', 'Measure']}
    />
  );
}
